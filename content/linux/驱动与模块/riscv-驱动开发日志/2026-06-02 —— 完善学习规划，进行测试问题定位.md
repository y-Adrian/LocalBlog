## 0.1 测试问题：
![[Pasted image 20260603033441.png]]

**问题定位**：fops 没有 `.llseek`，内核使用 `default_llseek` 但它更新的是 `f_pos`，而 `debris_read` 直接用 `*ppos`（即 `f_pos`）——所以 lseek 其实是有效的，真正的问题是 `debris_write` 将 `*ppos` 强制置 0，导致 **write 覆盖了 lseek 设置的值**。

**修复方式**：实际上问题在驱动：fops 没有注册 `.llseek`，内核用 `default_llseek` 但它对自定义设备不可靠。

修复完：
![[Pasted image 20260603034835.png]]
## 0.2 项目启动前自检
### 0.2.1 内核可以编译 ✅
### 0.2.2 ko 可以构建 ✅
### 0.2.3 内核版本匹配 ✅
```shell
root@ffa906c2e463:/home/work/Camera# modinfo output/debris.ko
filename:       /home/work/Camera/output/debris.ko
version:        0.1
description:    Progressive Driver Learning Project - milkv-duo-s
author:         Adrian
license:        GPL
srcversion:     AFEBF77B80739A497467EB4
depends:
name:           debris
vermagic:       5.10.4-tag- preempt mod_unload riscv
parm:           buf_size:kernel buffer size(Byte), default


[root@milkv-duo]~# uname -a
Linux milkv-duo 5.10.4-tag- #1 PREEMPT Fri May 30 14:51:54 CST 2025 riscv64 GNU/Linux
```

### 0.2.4 insmod/rmmod/dmesg OK ✅

### 0.2.5 kernel/dtb/modules 部署链路确认 

### 0.2.6 DTS 最小修改验证

### 0.2.7 I2C/GPIO 资源确认

### 0.2.8 摄像头 vendor sample 跑通

### 0.2.9 至少一路采集链路确认