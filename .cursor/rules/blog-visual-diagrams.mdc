---
description: 博客图文并茂——机制/数据流优先配图（Mermaid、ASCII、必要时静态图）
globs: content/**/*.md
alwaysApply: false
---

# 图文并茂（`content/**`）

撰写或改写博客正文时，**默认要画图**；文字解释「图里画不出的细节」，而不是用长段落代替图。

与 `.cursor/rules/blog-writing.mdc`（结构、术语、公式）和 `reason-then-action.mdc`（先因果再步骤）一并遵守。

## 最低配图量

| 篇章类型 | 建议 |
|----------|------|
| 机制 / 协议 / 内核 / 驱动 | 每个 **§ 主要机制** 至少 1 图；有状态迁移或调用链必画 |
| 教程 / 实践 | 数据路径、模块关系、时序各 1 图；步骤可配流程图 |
| 对比 / 选型 | 优先 **表格 + 一张对比流程或分层图** |
| 短答 / 流水（如 leetcode） | 用户未要求深度成文时可 0～1 图 |

单篇成文后自检：**能否删掉一半文字仍靠图读懂主干？** 若不能，补图或拆图。

## 图类型怎么选

| 要表达什么 | 用什么 |
|------------|--------|
| 模块依赖、分层、数据走向 | `flowchart`（LR/TB） |
| 函数/协议/中断调用先后 | `sequenceDiagram` |
| 状态机、生命周期 | `stateDiagram-v2` |
| 地址空间、栈/队列、单条链路 | 精简 **ASCII**（≤15 行） |
| 寄存器位域、真实 UI、波形 | **静态图** + alt 文本 |

复杂逻辑：**多张简单图** 优于一张塞满节点的巨图。

## Mermaid 写法（本站 Quartz）

- 代码块语言标 `mermaid`；图块**前后各空一行**，不与正文挤在同一段。
- 节点标签用中文或 `中文（English）`；避免在标签里写未转义的 `"`、`` ` ``。
- 时序图参与者名简短（`CPU`、`ISR`、`软中断`）；过长说明放图下正文。
- 构建后若图不显示：检查是否用了站点不支持的图表类型，改 `flowchart` / `sequenceDiagram` 或拆图。

**流程示例**：

```mermaid
flowchart LR
  A[硬中断 ISR] --> B[下半部]
  B --> C[进程上下文]
```

**时序示例**：

```mermaid
sequenceDiagram
  participant App
  participant Kernel
  App->>Kernel: read()
  Kernel-->>App: 数据就绪
```

## 图与正文的分工

1. **图**：主干因果、边界、谁调谁、数据从哪来到哪去。  
2. **正文**：参数、返回值、错误码、版本差异、与源码行号的对应。  
3. 图下用 **1～3 句** 点明「和实现/命令的对应」，不要重复抄写图里已有文字。

## 静态图片

- 路径放在站点约定目录（如 `quartz/static/...`），正文：`![简短 alt](相对路径)`。  
- alt 写清图意，便于无图环境与检索。  
- 能用 Mermaid 表达的逻辑结构，**优先 Mermaid**（易改、可 diff）。

## 反例 / 正例

| ❌ 反例 | ✅ 正例 |
|---------|---------|
| 连续多段纯文字描述「A→B→C→D」 | 一张 `flowchart` + 一段讲例外分支 |
| 一张图里 20+ 节点 | 拆成「总览图」+「细节图」 |
| 只有代码块、没有结构图 | 在代码前放「调用/内存」示意图 |
| 图与下一段代码块无空行 | 图块独立成段，便于渲染 |

## 交付前图示自检

- [ ] 主要机制是否都有对应图？  
- [ ] 时序/状态类内容是否用了 `sequenceDiagram` / `stateDiagram-v2`？  
- [ ] 图是否能在 `npx quartz build` 后正常显示（若改过 Mermaid）？  
- [ ] 术语首次出现是否仍遵守 `blog-writing.mdc` 的中英注解？
