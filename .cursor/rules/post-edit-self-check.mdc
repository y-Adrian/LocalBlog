---
description: 完成用户要求的修改后做一次简短自检，降低遗漏与构建失败
alwaysApply: true
---

# 修改完成后的自检（必做）

在用户要求的改动**交付前**，用最少步骤自检一遍；有任一项失败则先修再回复。

## 1. 与需求对齐

- 对照用户原话：是否多删、少做、理解偏了（尤其路径、`content/`、仅首页等条件）。
- 若动到 Quartz 组件或布局：是否仍符合 `slug` / `ConditionalRender` 等既有约定。

## 2. 静态检查

- 对**本次编辑过的文件**跑 linter / 类型诊断（若项目有），修掉由本次改动引入的问题。
- 快速扫一眼 diff：是否有误改、调试残留、未使用 import、半截字符串。

## 3. 可运行性（本仓库）

- 若改了 `quartz/`、`quartz.layout.ts`、`quartz.config.ts` 或会影响构建的脚本：在仓库根执行 **`npx quartz build`**，确认退出码为 0。
- 若仅改 `content/**/*.md` 且无语法特殊需求：至少确认无明显的 frontmatter / 链接断裂（按需抽查）。

## 4. 回复用户时

- 用一两句话说明**改了哪里、如何验证**（例如「本地 `npx quartz build` 已通过」），避免只写「好了」。

## 不必做

- 不为「自检」扩大范围（大段无关重构、全量测试），保持与本次任务一致。
