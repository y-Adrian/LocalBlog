---
description: 问「为什么/是什么」时先讲因果再方案；纯指令任务则直接执行
alwaysApply: true
---

# 何时用「原因 → 做法 → 示例」、何时不用

## 适用：用户在**提问**（求理解、求判断）

出现这类意图时，再按下面结构组织回答（除非用户明确说「只要结论」「只要代码」）：

- 典型语气：**为什么**、**是什么**、**有什么区别**、**合理吗**、**可能是什么原因**、**帮我分析一下** 等。
- 结构顺序：**原因与底层逻辑** → **可以怎么做 / 有哪些选项** → **示例**（命令、配置、改前改后等）。
- 简单问题：原因 1～2 句 + 做法 + 小示例即可；复杂问题仍保持顺序，可适当分段。

### 三节内容要点

1. **原因与底层逻辑**：现象背后的约束或机制；必要时点关键概念。  
2. **需要怎么做**：可选路径与推荐路径，若有取舍写一句理由。  
3. **示例**：可复制的命令/片段/对照，贴合本仓库（Quartz、`content/` 等）。

### 风格正例（提问场景）

先说明某配置在构建里起什么作用、不填会怎样，再给推荐值与注意点，最后给 `quartz.config.ts` 片段示例。避免只写「改某某字段就行」。

---

## 不适用：用户在**下指令**（要结果、要改动）

出现这类意图时，**不要套用**上述长结构；以**直接做完**为主，必要时用一两句话交代改了什么、怎么验证即可。

- 典型语气：**帮我**改/加/删/**实现**/**运行**/**构建**/**提交**/**创建文件**/**批量处理** 等明确交付物或动作的句子，且**没有**在追问「为什么」。
- 若指令里顺带问了原因，则**先答动作**，需要时再**简短**补一句原因，不必强行拆成三大段。

---

## 边界不清时

优先按**下指令**处理（先执行），执行后在回复末尾问一句是否需要展开原理即可。
