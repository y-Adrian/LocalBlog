---
description: 站内博客正文——问题驱动结构 + 尽量用图（Mermaid/示意）讲清原理
globs: content/**/*.md
alwaysApply: false
---

# 博客正文写作（`content/**`）

当用户在本仓库**撰写或改写博客类 Markdown**时，在遵守 `.cursor/rules/reason-then-action.mdc`（先因果再步骤与示例）的前提下，额外遵守本节。

## 叙事结构（建议顺序）

1. **场景与问题**：谁在什么约束下要解决什么；不解决会怎样。  
2. **读完能带走什么**：用 2～4 条 bullet 写清「结论/能力边界」。  
3. **核心概念**：只写本篇用得上的术语，并与问题绑定。  
4. **原理与机制**：因果链（输入 → 中间状态 → 输出）；写清**权衡**（性能/正确性/可移植性等）。  
5. **实践**：最短可复现路径 → 再展开变体；写明**环境/版本/期望现象**。  
6. **边界与排障**：误区、典型报错、检查顺序。  
7. **收尾**：检查清单或小表格（「何时选 A / B」）。

个人流水类目录（如 `content/leetcode/**`）：若用户未要求深度成文，可压缩篇幅，图示按需。

## 专用术语附英文

面向 **Linux / 嵌入式 / 网络 / 内核** 等专用术语，**首次出现**时附英文，便于对照文档与检索。

### 写法（统一）

- **推荐**：`中文术语（English）`  
- **有常用缩写**：`中文（缩写，English 全称）` — 例：中断服务例程（ISR，Interrupt Service Routine）  
- **API / 符号 / 命令**：保持 `` `epoll_wait` ``、`git log` 原文，**不必**再括英文；在旁用中文解释即可。  
- **已纯英文且业界通用的**（TCP、HTTP、Git、Makefile）：首次可写 `TCP（Transmission Control Protocol）`，后文直接用缩写；不必每词都括。

### 范围

| 要写 | 可不写 |
|------|--------|
| 内核/驱动概念（硬中断、下半部、软中断、自旋锁） | 日常用语（文件、目录、错误） |
| 体系结构/协议专名（GIC、NUMA、mbuf） | 正文已全是英文的代码块 |
| 机制名（中断嵌套、粘包、零拷贝） | 同一篇后文重复出现的同一术语 |

同一篇文章里，**同一术语只注解一次**（后文直接用中文或缩写）；章节很长时可在该章首次再注一次。

### 示例

- ✅ 远程过程调用（RPC，Remote Procedure Call）  
- ✅ 多路复用（I/O multiplexing；实现如 `epoll`）  
- ❌ 中断（Interrupt）里不能睡眠（Interrupt）——不要重复括注  
- ❌ 使用 `` `mutex_lock`（mutex lock）`` —— API 名不括英文

## 图文并茂（优先顺序）

- **每个主要机制 / 数据流 / 调用顺序**，尽量配**至少一张图**，避免大段文字硬啃。  
- **首选 Mermaid**（与本站 Quartz 常见能力匹配）：`flowchart` / `sequenceDiagram` / `stateDiagram-v2` 等；复杂时拆成多张简单图。  
- **次选**：精简 **ASCII 结构图**（模块、队列、地址空间、分层）。  
- **确需截图或手绘**：说明应放入的路径（如 `quartz/static/...` 或站点约定目录），在正文用 Markdown 图片引用，并写一句 alt 文本。  
- 图前后各保留**一行空行**；代码块与图**不要**挤在同一段落里。

## 反例 / 正例（图示）

**反例**：连续 30 行文字描述状态机，无图。  
**正例**：用 8～15 行 `mermaid` 状态图或时序图展示状态迁移，再用 1 段话解释「与实现细节的对应关系」。

## 篇幅与拆分

- 单篇过长时：拆成系列篇，每篇仍保持「问题 → 图 → 文字解释 → 可操作步骤」闭环。
